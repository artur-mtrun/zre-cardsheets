<!DOCTYPE html>
<html lang="pl">
<head>
    <%- include('../includes/head.ejs') %>
    <link rel="stylesheet" href="/css/machines.css">
</head>

<body>
    <%- include('../includes/navigation.ejs') %>
    <main class="container mt-4">
        <h1 class="mb-4 centered-heading">Lista maszyn</h1>
        
        <% if (machines && machines.length > 0) { %>
            <table class="table table-striped table-hover">
                <thead class="table-success">
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Nr maszyny</th>
                        <th scope="col">Opis</th>
                        <th scope="col">Obszar</th>
                        <th scope="col">IP</th>
                        <th scope="col">Port</th>
                        <th scope="col">Akcje</th>
                    </tr>
                </thead>
                <tbody>
                    <% machines.forEach((machine, index) => { %>
                        <tr>
                            <th scope="row"><%= index + 1 %></th>
                            <td><%= machine.machinenumber %></td>
                            <td><%= machine.descript %></td>
                            <td><%= machine.area_id %></td>
                            <td><%= machine.ip %></td>
                            <td><%= machine.port %></td>
                            <td>
                                <a href="/machines/<%= machine.machine_id %>" class="btn btn-info btn-sm">Szczegóły</a>
                                <a href="/machines/edit/<%= machine.machine_id %>" class="btn btn-warning btn-sm">Edytuj</a>
                                <form action="/machines/delete" method="POST" class="d-inline">
                                    <input type="hidden" value="<%= machine.machine_id %>" name="machineId">
                                    <button class="btn btn-danger btn-sm" type="submit" onclick="return confirm('Czy na pewno chcesz usunąć tę maszynę?')">Usuń</button>
                                </form>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } else { %>
            <div class="alert alert-info" role="alert">
                Brak maszyn w systemie.
            </div>
        <% } %>
        <a href="/machines/new" class="btn btn-primary">Dodaj nową maszynę</a>
    </main>

<%- include('../includes/end.ejs') %>
</body>
</html>
