<%- include('../includes/head.ejs') %>

</head>

<body>
    <%- include('../includes/navigation.ejs') %>
    <main class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="card shadow">
                    <div class="card-body">
                        <h1 class="card-title text-center mb-4">Dodaj nową firmę</h1>
                        <form action="/add-company" method="POST" class="needs-validation" novalidate>
                            <div class="mb-3">
                                <label for="company_descript" class="form-label">Opis firmy</label>
                                <textarea class="form-control" id="company_descript" name="company_descript" rows="3" required></textarea>
                                <div class="invalid-feedback">
                                    Proszę podać opis firmy.
                                </div>
                            </div>
                            <div class="d-grid gap-2 mt-4">
                                <button type="submit" class="btn btn-primary">Dodaj firmę</button>
                                <a href="/company-list" class="btn btn-secondary">Anuluj</a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <script>
    // Skrypt do walidacji formularza po stronie klienta
    (function () {
        'use strict'
        var forms = document.querySelectorAll('.needs-validation')
        Array.prototype.slice.call(forms)
            .forEach(function (form) {
                form.addEventListener('submit', function (event) {
                    if (!form.checkValidity()) {
                        event.preventDefault()
                        event.stopPropagation()
                    }
                    form.classList.add('was-validated')
                }, false)
            })
    })()
    </script>

<%- include('../includes/end.ejs') %>

